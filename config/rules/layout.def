# https://developer.mozilla.org/en-US/docs/Web/CSS/float

all := initial | inherit | unset

float := left | right | none | inline-start | inline-end
clear := none | left | right | both | inline-start | inline-end

display := none | inline | block | list-item | inline-list-item
 | inline-block | inline-table | table | table-cell | table-column
 | table-column-group | table-footer-group | table-header-group
 | table-row | table-row-group | flex | inline-flex | grid
 | inline-grid | run-in | ruby | ruby-base | ruby-text
 | ruby-base-container | ruby-text-container | contents

left
right
bottom
top := <length> | <percentage> | auto

position := static | relative | absolute | sticky | fixed

overflow-x
overflow-y
overflow := visible | hidden | scroll | auto

visibility := visible | hidden | collapse

z-index := auto | <integer>

padding := [ <length> | <percentage> ]{1,4}
padding-bottom
padding-left
padding-right
padding-top := <length> | <percentage>

margin := [ <length> | <percentage> | auto ]{1,4}

margin-bottom
margin-left
margin-right
margin-top := <length> | <percentage> | auto

<width> = [ [ <length> | <percentage> ] && [ border-box | content-box ]? ]
  | available | min-content | max-content | fit-content | auto

height
width := [ [ <length> | <percentage> ] && [ border-box | content-box ]? ]
  | available | min-content | max-content | fit-content | auto

min-width
min-height := <length> | <percentage> | auto | max-content | min-content | fit-content | fill-available

max-width
max-height := <length> | <percentage> | none | max-content | min-content | fit-content | fill-available


border-collapse := collapse | separate
border-spacing  := <length> <length>?
caption-side    := top | bottom | block-start | block-end | inline-start | inline-end
empty-cells     := show | hide
table-layout    := auto | fixed
vertical-align  := baseline | sub | super | text-top | text-bottom | middle | <length> | <percentage>
  | top | bottom


align-content := flex-start | flex-end | center | space-between | space-around | stretch
align-items := flex-start | flex-end | center | baseline | stretch
align-self := auto | flex-start | flex-end | center | baseline | stretch

<flex-shrink>
<flex-grow>
flex-shrink
flex-grow := <number>

<flex-basis> = content | <width>
flex-basis := content | <width>
flex-direction := row | row-reverse | column | column-reverse
flex := none | [ <flex-grow> <flex-shrink>? || <flex-basis> ]
flex-wrap := nowrap | wrap | wrap-reverse

box-sizing := content-box | border-box
